# Core dependencies
yfinance>=0.2.31
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0

# Note: ffmpeg must be installed separately on your system for MP4 animation export.
# Windows  : winget install ffmpeg   OR  choco install ffmpeg
# macOS    : brew install ffmpeg
# Linux    : sudo apt install ffmpeg
